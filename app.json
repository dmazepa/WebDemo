{
  "environments": {
    "test": {
      "scripts": {
        "install": "pip install robotframework",
        "test": "python foo.py"
        //"test": "robot login_tests/valid_login.robot"
      }
    }
  }
}